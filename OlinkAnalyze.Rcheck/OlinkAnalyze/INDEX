check_checksum          Help function comparing the checksum reported
                        by Olink software to the checksum of the
                        delivered NPX file.
check_file_exists       Help function checking if file exists.
check_is_arrow_object   Help function to check if a data frame is an R6
                        ArrowObject.
check_is_boolean        Help function checking if a variable is a
                        boolean vector.
check_is_character      Help function checking if a variable is a
                        character vector.
check_is_data_frame     Help function to check if the variable is a
                        data frame.
check_is_integer        Help function checking if a variable is an
                        integer vector.
check_is_numeric        Help function checking if a variable is a
                        numeric vector.
check_is_scalar_boolean
                        Help function checking if a variable is a
                        boolean vector of length 1.
check_is_scalar_character
                        Help function checking if a variable is a
                        character vector of length 1.
check_is_scalar_integer
                        Help function checking if a variable is an
                        integer vector of length 1.
check_is_scalar_numeric
                        Help function checking if a variable is a
                        numeric vector of length 1.
check_is_tibble         Help function to check if a data frame is a
                        tibble data frame.
check_library_installed
                        Help function to check if suggested libraries
                        are installed when required.
check_out_df_arg        Help function checking that the requested
                        output class of the read_npx* functions is
                        acceptable.
convert_read_npx_output
                        Help function converting the output data frame
                        from the read_npx* function to a tibble or an
                        ArrowObject.
get_checksum_file       Help function to get the name of the checksum
                        file from a zip NPX.
get_field_separator     Help function to get the file separator.
get_npx_file            Help function to get the name of the NPX file
                        from a compresseed NPX.
manifest                Example Sample Manifest
npx_data1               NPX Data in Long format.
npx_data2               NPX Data in Long format, a follow-up.
read_npx                Function to read NPX data into R in long
                        format.
read_npx_delim          Help function to read in Olink data from comma
                        or semicolon delimited files.
read_npx_parquet        Help function to read Olink parquet files.
read_npx_zip            Helper function to read NPX zip-compressed
                        files with checksum.
