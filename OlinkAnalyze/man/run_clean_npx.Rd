% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/olink_npx.R
\name{run_clean_npx}
\alias{run_clean_npx}
\title{Quietly clean proteomics data quantified with Olink's PEA technology}
\usage{
run_clean_npx(df, ...)
}
\arguments{
\item{df}{A "tibble" or "ArrowObject"
from \code{\link{read_npx}}. The output of \code{\link[=read_npx]{read_npx()}} is an \code{olink_npx}
object that carries the check log as an attribute. If the check log
indicates that the data still needs cleaning (e.g. invalid identifiers,
duplicate samples), the downstream function will automatically clean the
data before proceeding.}
}
\value{
Dataset,
"tibble" or "ArrowObject", with Olink
data in long format.
}
\description{
Internal wrapper around \code{\link[=clean_npx]{clean_npx()}} that runs the same cleaning pipeline
while suppressing messages and warnings emitted during processing.

Unlike \code{\link[=clean_npx]{clean_npx()}}, this function is intended for internal use in cases
where a quiet cleaning step is needed. If rows are removed, a single
\code{\link[cli:cli_alert]{cli::cli_alert()}} message is shown. If no rows are removed, no output is
printed.
}
\details{
This function forwards all arguments to \code{\link[=clean_npx]{clean_npx()}} and forces
\code{verbose = FALSE}.

If any rows are removed during cleaning, the function prints a single alert
indicating how many entries were removed and instructing the user to run
\code{\link[=clean_npx]{clean_npx()}} directly to inspect which rows were removed.
}
\author{
Kang Dong
Klev Diamanti
}
\keyword{internal}
