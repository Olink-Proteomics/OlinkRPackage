% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/olink_class.R
\name{strip_check_log}
\alias{strip_check_log}
\title{Strip check log from an Olink data object}
\usage{
strip_check_log(df)
}
\arguments{
\item{df}{An \code{olink_class} tibble or an ArrowObject with \code{olink_check_log}
metadata.}
}
\value{
The data without the check log: a plain tibble (\code{tbl_df}) when given
an \code{olink_class} tibble, or the ArrowObject with the \code{olink_check_log}
metadata key removed when given an ArrowObject. Other inputs are returned
unchanged.
}
\description{
Removes the attached check log from either an \code{olink_class} tibble or an
ArrowObject. For tibbles this converts the object to a plain tibble (removing
the \code{olink_class} class and the \code{check_log} attribute). For ArrowObjects it
removes the \code{olink_check_log} metadata key. If the input carries no check
log, it is returned unchanged.
}
\examples{
\dontrun{
# read NPX data (returns olink_class object)
  npx_file <- system.file(
    "extdata",
    "npx_data_ext.parquet",
    package = "OlinkAnalyze"
  )

  npx_obj <- OlinkAnalyze::read_npx(
    filename = npx_file
  )

  # strip check log, returning a plain tibble
  npx_tbl <- OlinkAnalyze::strip_check_log(
    df = npx_obj
  )

  class(npx_tbl)

  OlinkAnalyze::olink_check_log(
    df = npx_tbl
  )
}

}
