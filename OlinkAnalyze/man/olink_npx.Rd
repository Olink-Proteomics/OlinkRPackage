% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/olink_npx.R
\name{olink_npx}
\alias{olink_npx}
\title{S3 class for Olink NPX data with attached check log}
\description{
The \code{olink_npx} class is a tibble subclass that carries the output of
\code{\link[=check_npx]{check_npx()}} as an attribute. This allows downstream functions to
automatically access the check log without the user having to pass it
explicitly.

For ArrowObjects, check log metadata is stored in the Arrow table's
schema-level metadata under the key \code{"olink_check_log"} as a serialized
JSON string.
}
\details{
The class is designed to be fully backward-compatible. All downstream
functions that accept \code{check_log = NULL} will first attempt to extract the
check log from the data object itself. If not found, they fall back to the
existing behavior of running \code{\link[=check_npx]{check_npx()}} internally.

The \code{olink_npx} class survives most dplyr operations (e.g.
\code{\link[dplyr:filter]{dplyr::filter()}}, \code{\link[dplyr:mutate]{dplyr::mutate()}}, \code{\link[dplyr:select]{dplyr::select()}}) through the
\code{\link[dplyr:dplyr_extending]{dplyr::dplyr_reconstruct()}} mechanism. Note that operations that combine
multiple data frames (e.g. \code{\link[dplyr:bind_rows]{dplyr::bind_rows()}}) may strip the class and
check log.
}
\keyword{internal}
