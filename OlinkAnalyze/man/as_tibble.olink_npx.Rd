% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/olink_npx.R
\name{as_tibble.olink_npx}
\alias{as_tibble.olink_npx}
\title{Convert an \code{olink_npx} object to a plain tibble}
\usage{
\method{as_tibble}{olink_npx}(x, ...)
}
\arguments{
\item{x}{An \code{olink_npx} object.}

\item{...}{Additional arguments (currently ignored).}
}
\value{
A tibble (\code{tbl_df}) without the \code{olink_npx} class or the check log
attribute.
}
\description{
Strips the \code{olink_npx} class and removes the attached check log, returning a
plain tibble. This is useful for testing or when the user wants to remove the
extra metadata carried by the \code{olink_npx} class.
}
\examples{
\dontrun{
# read NPX data (returns olink_npx object)
npx_file <- system.file("extdata",
                        "npx_data_ext.parquet",
                        package = "OlinkAnalyze")
npx_obj <- read_npx(filename = npx_file)

# class includes olink_npx
class(npx_obj)

# convert to plain tibble
npx_tbl <- tibble::as_tibble(npx_obj)

# class is now a plain tibble
class(npx_tbl)

# check_log attribute is gone
olink_check_log(npx_tbl)
}

}
