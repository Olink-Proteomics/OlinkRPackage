% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_npx.R
\name{clean_control_sample_id}
\alias{clean_control_sample_id}
\title{Remove Control Samples Based on Sample ID Pattern}
\usage{
clean_control_sample_id(
  df,
  check_npx_log,
  control_sample_id = c("control"),
  out_df = "tibble"
)
}
\arguments{
\item{df}{A data frame loaded using \code{read_npx()}, including a sample ID column.}

\item{check_npx_log}{A list generated by \code{check_npx()} that includes:
\itemize{
\item \code{col_names$sample_id}: the column name in \code{df} identifying sample IDs.
}}

\item{control_sample_id}{A character vector of regex strings
(case-insensitive) used to match control sample IDs. Default: \code{"control"}.}

\item{out_df}{Output format of the returned data frame.
Options: \code{"tibble"} or \code{"arrow"}. Default is \code{"tibble"}.}
}
\value{
A filtered data frame with control sample IDs removed.
}
\description{
This function removes rows from a data frame where the \code{sample_id} matches a
regular expression pattern indicating control samples. This is useful for filtering
out technical controls or reference samples based on their identifiers.
}
\examples{
# Assuming df has a "SampleID" column, and control samples contain "Control"
clean_df <- clean_control_sample_id(df, check_npx_log, control_sample_id = c("control"))
}
\author{
Kang Dong
}
