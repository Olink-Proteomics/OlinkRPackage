# vector of bridge recommendations assigned to each datapoint from the
# cross-product normalization method and when setting the format 'format'
# argument to TRUE.
bridge_recommendations <- c("MedianCentering",
                            "QuantileSmoothing",
                            "NotBridgeable",
                            "NotOverlapping")
